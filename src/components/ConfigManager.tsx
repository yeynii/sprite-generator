import styled from "styled-components";

const ConfigManager = () => {
  return <Container>ConfigManager</Container>;
};
const Container = styled.div`
  min-width: 300px;
  min-height: 300px;
  padding: 10px;
  border: 1px solid black;
`;

export default ConfigManager;
